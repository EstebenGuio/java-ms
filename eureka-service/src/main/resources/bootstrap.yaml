spring:
  application:
    name: eureka-server
  cloud:
    config:
      enabled: true
      uri: http://0.0.0.0:8000
  freemarker:
    template-loader-path: classpath:/templates/
    prefer-file-system-access: false
